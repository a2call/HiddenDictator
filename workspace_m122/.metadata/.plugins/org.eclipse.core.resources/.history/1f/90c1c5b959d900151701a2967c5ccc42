package ch.dnisler.hiddendictator;

import javax.servlet.http.HttpSession;

public class Util {
	public static String getPage(HttpSession session){
		if(session.getAttribute("status").equals("registered") ){
			return "lobbylist";
		}else if(session.getAt)
		else{
			return "home";
		}
	}
}
