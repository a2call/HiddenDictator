package ch.dnisler.hiddendictator;

import java.util.HashMap;

public class Server {
	private static HashMap<String, User> userlist = new HashMap<>();
	
	public static boolean addUser(String username){
		userlist.put(username, new User());
	}
}
